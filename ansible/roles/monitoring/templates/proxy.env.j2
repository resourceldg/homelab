# {{ ansible_managed }}
CADDY_BASE_DOMAIN={{ caddy_base_domain }}
ACME_EMAIL={{ caddy_acme_email }}
DUCKDNS_TOKEN={{ vault_duckdns_token }}
# Subdominio del tablero del pañol (rol panol). Va acá porque el que lo usa es
# el Caddyfile; el default vale mientras el rol panol no lo cambie.
PANOL_SUBDOMAIN={{ panol_subdomain | default('panol') }}
