# {{ ansible_managed }}
CADDY_BASE_DOMAIN={{ caddy_base_domain }}
ACME_EMAIL={{ caddy_acme_email }}
DUCKDNS_TOKEN={{ vault_duckdns_token }}
# Networks allowed to reach the "private" Caddy sites (Prometheus, cAdvisor).
TAILSCALE_CIDR={{ tailscale_cidr }}
LAN_CIDR={{ lan_cidr }}
